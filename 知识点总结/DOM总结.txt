DOM用处：动态实现页面变化效果







元素和节点本质是一个东西，只是元素是一个统称，在HTML中元素指的是标签、文本等的东西。节点是一个唯一的东西，不是泛指，只是出现在“树”这样一个结构中的产物，   于元素来说有元素节点，于文本来说有文本节点，于属性来说有属性节点。本质与元素没有区别。

对于兄弟元素的修改，不可以直接取到元素节点进行修改，而是使用innerHTML进行修改。

节点树和元素树的区别在于：节点树包含了文本（在代码中出现的空行），元素树不包含文本和注释

getAttrbute和setAttrbute方法

